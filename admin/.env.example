# Where the admin panel reads data from.
# Local dev: leave unset — Vite proxies /api to http://localhost:8000.
# Production (Render): set this to the backend's public API base, e.g.
# VITE_API_BASE=https://barber-api.onrender.com/api/v1
VITE_API_BASE=
